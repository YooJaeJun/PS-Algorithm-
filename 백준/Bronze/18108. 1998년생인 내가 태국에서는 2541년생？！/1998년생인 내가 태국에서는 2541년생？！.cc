#include <iostream>
#include <vector>
using namespace std;

void solution() {
	int y;
	cin >> y;
	cout << y - (2541 - 1998);
}

int main() {
	ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	solution();
	return 0;
}